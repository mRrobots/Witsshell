#include <stdio.h>
#include <stdlib.h>

int main()
{
    // freopen("lol.txt","w",stdout);
    // printf("Send it to lol right now. ");
    // fclose(stdout);
    int i =1;

    if(i){
        printf("bool works\n");
    }
    else{
        printf("Na\n");
    }

    return(0);
}